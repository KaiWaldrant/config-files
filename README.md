# Instructions for settings

## Biome

1. Copy the `biome.json` file into your project.
2. Add the `lint-format` setting to `scripts` section in the `pacakge.json` file.
3. When using VS code update your settings for biome with the rules in `.vscode/settings.json`.
